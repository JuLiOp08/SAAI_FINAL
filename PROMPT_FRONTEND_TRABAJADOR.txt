# PROMPT PARA IA - FRONTEND SAAI TRABAJADOR (React Web)

## 🎯 CONTEXTO DEL PROYECTO

Estoy desarrollando una aplicación web React para **TRABAJADORES DE TIENDA** del sistema SAAI (Smart Assistant for Inventory). Esta es 1 de 3 aplicaciones del ecosistema.

**Rol del usuario:** TRABAJADOR/WORKER
- Permisos: Gestión de productos, registro de ventas, consulta de notificaciones
- Sin acceso a: Gastos, analítica, reportes, usuarios, predicciones ML

**Backend:** AWS Serverless (API Gateway + Lambda + DynamoDB)
- Autenticación: JWT (Bearer token) - el token se envía en CADA request
- Multi-tenant: Cada trabajador pertenece a UNA tienda (tenant_id extraído del JWT por el backend)
- WebSockets: Notificaciones en tiempo real

**Frontend:** React + Vite + Tailwind CSS
- Tailwind CSS para estilos (ya configurado en package.json)
- React Router para navegación
- Axios para llamadas API
- Context API para estado global

---

## 📚 ARCHIVOS DE REFERENCIA OBLIGATORIOS

**IMPORTANTE:** Antes de escribir cualquier código, debes leer y entender completamente estos documentos:

### 1. SAAI_oficial.txt
- **Sección crítica:** "2. Arquitectura TRABAJADOR"
  - 2.1 Endpoints (Request/Response) - Líneas 82-510
  - 2.2 Lambdas (qué hace cada una) - Líneas 512-705
- **Convenciones del backend:** Líneas 1-80
  - Autenticación y JWT
  - Multi-tenant (tenant_id)
  - Modelo DynamoDB (tenant_id + entity_id + data)
  - Códigos vs UUIDs (el frontend SOLO usa códigos: T001P001, T001V005, etc.)
  - Fecha/Hora Perú (backend decide timestamps)
  - Paginación (limit + next_token)

### 2. SAAI_resumen_tecnico.txt
- Arquitectura general del sistema
- Flujo de autenticación
- WebSockets y eventos en tiempo real
- Persistencia y modelo de datos

### 3. SAAI_Backend_Complete.postman_collection.json
- **Usar como referencia para:**
  - Request bodies exactos (estructura JSON)
  - Response esperados (estructura de datos)
  - Headers requeridos (Authorization: Bearer)
  - Variables de colección (base_url, tokens, etc.)
- **Módulos relevantes para TRABAJADOR:**
  - 1. AUTH → Login Worker
  - 3. PRODUCTOS → Crear, Listar, Buscar, Actualizar, Eliminar
  - 4. VENTAS → Calcular Monto, Registrar, Listar, Buscar
  - 9. NOTIFICACIONES → Listar

---

## 🏗️ ESTRUCTURA DE LA APP

**Revisa la plantilla React + Vite que he creado** antes de empezar. La estructura debe ser:

```
saai-trabajador/
├── src/
│   ├── pages/            # Páginas principales (rutas)
│   │   ├── auth/
│   │   │   └── LoginPage.jsx
│   │   ├── productos/
│   │   │   ├── ProductosPage.jsx          # Lista + crear
│   │   │   ├── EditarProductoPage.jsx
│   │   │   └── BuscarProductosPage.jsx
│   │   ├── ventas/
│   │   │   ├── RegistrarVentaPage.jsx
│   │   │   ├── VentasPage.jsx             # Lista + buscar
│   │   │   └── BuscarVentasPage.jsx
│   │   ├── notificaciones/
│   │   │   └── NotificacionesPage.jsx
│   │   └── HomePage.jsx                   # Dashboard simple
│   ├── components/       # Componentes reutilizables
│   │   ├── layout/
│   │   │   ├── Navbar.jsx                 # Navegación superior
│   │   │   ├── Sidebar.jsx                # Menú lateral
│   │   │   └── Layout.jsx                 # Wrapper con Navbar + Sidebar
│   │   ├── ui/                            # Componentes UI base
│   │   │   ├── Button.jsx                 # Botón con Tailwind
│   │   │   ├── Input.jsx                  # Input con Tailwind
│   │   │   ├── Modal.jsx
│   │   │   ├── Table.jsx
│   │   │   ├── Card.jsx
│   │   │   ├── Badge.jsx
│   │   │   ├── SearchBar.jsx
│   │   │   └── Spinner.jsx                # Loading spinner
│   │   ├── productos/
│   │   │   ├── ProductCard.jsx
│   │   │   ├── ProductForm.jsx
│   │   │   └── ProductTable.jsx
│   │   ├── ventas/
│   │   │   ├── VentaCard.jsx
│   │   │   ├── VentaForm.jsx
│   │   │   └── CarritoVenta.jsx           # Carrito para registrar venta
│   │   └── notificaciones/
│   │       ├── NotificationBadge.jsx
│   │       └── NotificationList.jsx
│   ├── services/         # Lógica de API y WebSocket
│   │   ├── api.js        # Axios instance con interceptors
│   │   ├── auth.service.js      # Login, logout, token management
│   │   ├── productos.service.js # CRUD productos
│   │   ├── ventas.service.js    # Registrar/listar ventas
│   │   └── websocket.service.js # WebSocket connection
│   ├── context/          # Estado global (Context API)
│   │   ├── AuthContext.jsx
│   │   └── NotificationContext.jsx
│   ├── hooks/            # Custom hooks
│   │   ├── useAuth.js
│   │   ├── useWebSocket.js
│   │   └── usePagination.js
│   ├── utils/
│   │   ├── constants.js  # API_URL, categorías, métodos pago
│   │   └── helpers.js    # Formateo de fecha, moneda, validaciones
│   ├── routes/
│   │   ├── AppRoutes.jsx # Rutas protegidas (requieren auth)
│   │   └── ProtectedRoute.jsx # HOC para rutas privadas
│   └── App.jsx           # Router + AuthProvider
├── index.html
├── tailwind.config.js    # Configuración de Tailwind
├── vite.config.js
└── package.json          # Incluye Tailwind CSS
```

---

## 🚀 ORDEN DE IMPLEMENTACIÓN SUGERIDO

### FASE 1: Autenticación y Navegación (COMENZAR AQUÍ)
1. **LoginPage** 
   - POST /login con email + password
   - Guardar token en localStorage (NO AsyncStorage, esto es web)
   - **IMPORTANTE:** El tenant_id NO se guarda manualmente, el backend lo extrae del JWT automáticamente
   - Extraer codigo_usuario del response para mostrar en UI
   - Navegación a HomePage después de login exitoso
   - UI con Tailwind: formulario centrado, card con sombra, botón primary

2. **AuthContext**
   - Estado global: { user, token, isAuthenticated }
   - **NO guardes tenant_id** en estado (el backend lo maneja desde el JWT)
   - Funciones: login(), logout(), checkAuth()
   - Persistencia de sesión (localStorage)
   - Inicializar token desde localStorage al cargar app

3. **API Service (api.js)**
   - Axios instance con baseURL
   - Interceptor request: agregar Authorization: Bearer {token} desde localStorage
   - Interceptor response: manejar errores 401 → logout automático + redirect a /login
   - Mantener el response completo (no aplanar data.data automáticamente). Usar helper opcional getData(res) si es necesario para acceder a res.data.data

### FASE 2: Productos (Core Functionality)
4. **ProductosPage**
   - GET /productos con paginación (limit + next_token)
   - Tabla con Tailwind (o grid de cards)
   - Botón "Crear Producto" abre modal o navega a formulario
   - Mostrar: código, nombre, precio, stock, categoría, acciones (editar/eliminar)
   - Infinite scroll o botón "Cargar más" para next_token
   - **IMPORTANTE:** El tenant_id NO se envía en la request, el backend lo extrae del JWT

5. **Formulario Crear Producto** (modal o página separada)
   - Campos: nombre, precio, stock, categoría, descripción
   - POST /productos (body NO incluye tenant_id, el backend lo agrega automáticamente)
   - Validaciones: precio > 0, stock >= 0, nombre requerido
   - Categorías fijas: bebidas, snacks, abarrotes, limpieza, otros (select con Tailwind)
   - UI Tailwind: formulario con labels, inputs estilizados, botones primary/secondary

6. **BuscarProductosPage**
   - POST /productos/buscar con query
   - SearchBar con debounce
   - Búsqueda por: nombre, código, categoría

7. **EditarProductoPage**
   - PUT /productos/{codigo_producto}
   - Pre-fill del formulario con datos actuales
   - Solo editar: precio, stock, descripción (nombre y categoría no se cambian)

### FASE 3: Ventas (Critical Feature)
8. **RegistrarVentaPage**
   - Selector de productos (múltiple)
   - Cantidad por producto
   - POST /ventas/calcular (preview del total SIN guardar)
   - POST /ventas (registrar venta final)
   - Selector de método de pago: efectivo, tarjeta, yape
   - Mostrar total calculado antes de confirmar

9. **ListaVentasPage**
   - GET /ventas con paginación
   - Mostrar: código, fecha, cliente, total, método pago
   - Filtros: fecha (hoy, esta semana, este mes)

10. **BuscarVentasPage**
    - POST /ventas/buscar
    - Búsqueda por: código, fecha, método de pago, producto vendido

### FASE 4: Notificaciones y WebSocket
11. **NotificacionesPage**
    - GET /notificacion con paginación
    - Lista de alertas del sistema
    - Badge con contador de no leídas

12. **WebSocket Service**
    - Conectar a WebSocket API al login usando el formato EXACTO documentado en SAAI_oficial.txt y Postman (wss://...?token=JWT)
    - Escuchar eventos: venta_registrada, producto_actualizado
    - Mostrar notificaciones push nativas
    - Actualizar badge de notificaciones

### FASE 5: Pulido y UX
13. **Mejoras generales**
    - Loading states (spinner de Tailwind o biblioteca como react-loader-spinner)
    - Error handling (react-hot-toast para mensajes)
    - Validaciones de formularios
    - Confirmaciones antes de eliminar (diálogo personalizado o biblioteca como react-modal)
    - Botón "Cargar más" o infinite scroll en listas paginadas
    - Web Notifications API (opcional, si el usuario da permiso del navegador)

---

## 💡 TIPS IMPORTANTES

### 1. Autenticación y Multi-tenant
```javascript
// ✅ CORRECTO: El token se guarda en localStorage (web)
const login = async (email, password) => {
  const response = await api.post('/login', { email, password });
  const { token, user } = response.data.data;
  
  // Guardar en localStorage (NO AsyncStorage)
  localStorage.setItem('token', token);
  localStorage.setItem('user', JSON.stringify(user));
  
  setAuthState({ token, user, isAuthenticated: true });
  
  // ❌ NO HACER ESTO: NO guardes tenant_id manualmente
  // localStorage.setItem('tenant_id', user.tenant_id); // ❌ INCORRECTO
  
  // El tenant_id está en el JWT, el BACKEND lo extrae automáticamente
};

// ✅ CORRECTO: Axios interceptor agrega el token automáticamente
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
    // ❌ NO agregues tenant_id manualmente en headers
  }
  return config;
});

// ✅ CORRECTO: Manejar 401 (token expirado)
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      localStorage.removeItem('user');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 2. Códigos vs UUIDs
```javascript
// ❌ INCORRECTO: Nunca uses UUID
const eliminar = (uuid) => api.delete(`/productos/${uuid}`);

// ✅ CORRECTO: Usa siempre códigos (T001P001, T001V005, etc.)
const eliminar = (codigo_producto) => api.delete(`/productos/${codigo_producto}`);
```

### 3. Paginación
```javascript
const [productos, setProductos] = useState([]);
const [nextToken, setNextToken] = useState(null);

const fetchProductos = async () => {
  const params = { limit: 20 };
  if (nextToken) params.next_token = nextToken;
  
  const response = await api.get('/productos', { params });
  setProductos([...productos, ...response.data.data.productos]);
  setNextToken(response.data.data.next_token || null);
};
```

### 4. Fecha/Hora
```javascript
// ❌ INCORRECTO: No envíes fecha/hora desde el frontend
const venta = {
  productos: [...],
  fecha: new Date().toISOString() // ❌ NO HACER ESTO
};

// ✅ CORRECTO: El backend genera la fecha automáticamente
const venta = {
  productos: [...],
  metodo_pago: 'efectivo'
  // fecha NO se envía
};
```

### 5. WebSockets (Notificaciones en Tiempo Real)
```javascript
// Conectar al login
const connectWebSocket = (token) => {
  const ws = new WebSocket(`wss://xxx.execute-api.us-east-1.amazonaws.com/dev?token=${token}`);
  
  ws.onopen = () => {
    console.log('✅ WebSocket conectado');
  };
  
  ws.onmessage = (event) => {
    const { event_type, payload } = JSON.parse(event.data);
    
    if (event_type === 'venta_registrada') {
      // Mostrar notificación browser (Web Notifications API)
      if (Notification.permission === 'granted') {
        new Notification('Nueva venta registrada', {
          body: `Venta ${payload.codigo_venta} registrada exitosamente`,
          icon: '/favicon.ico'
        });
      }
      
      // Refetch lista de ventas
      fetchVentas();
    }
    
    if (event_type === 'producto_actualizado') {
      // Refetch lista de productos
      fetchProductos();
    }
  };
  
  ws.onerror = (error) => {
    console.error('❌ WebSocket error:', error);
  };
  
  ws.onclose = () => {
    console.log('WebSocket desconectado');
    // Reconectar después de 5 segundos
    setTimeout(() => connectWebSocket(token), 5000);
  };
  
  return ws;
};

// Solicitar permisos de notificaciones al login
if (Notification.permission === 'default') {
  Notification.requestPermission();
}
```

### 6. Validaciones
```javascript
// Validar antes de enviar al backend
const validarProducto = (data) => {
  const errors = {};
  
  if (!data.nombre || data.nombre.trim() === '') {
    errors.nombre = 'Nombre es requerido';
  }
  if (!data.precio || data.precio <= 0) {
    errors.precio = 'Precio debe ser mayor a 0';
  }
  if (data.stock === '' || data.stock === null || data.stock === undefined || data.stock < 0) {
    errors.stock = 'Stock no puede ser negativo';
  }
  
  return Object.keys(errors).length === 0 ? null : errors;
};
```

### 7. Error Handling
```javascript
// Manejar errores de API consistentemente
try {
  const response = await api.post('/productos', data);
  toast.success('Producto creado correctamente');
  navigate(-1); // o navigate('/productos')
} catch (error) {
  const mensaje = error.response?.data?.message || 'Error al crear producto';
  toast.error(mensaje);
}
```

---

## 📱 FEATURES ESPECÍFICAS TRABAJADOR

### Productos
- ✅ Crear, editar, eliminar, listar, buscar
- ✅ Categorías predefinidas (no custom)
- ✅ Stock siempre >= 0
- ✅ Precio siempre > 0

### Ventas
- ✅ Calcular total ANTES de registrar (POST /ventas/calcular)
- ✅ Descuento de stock automático (backend)
- ✅ Métodos de pago: efectivo, tarjeta, yape
- ✅ Cliente opcional (default: "Cliente General")
- ✅ No se pueden editar ni eliminar ventas (solo listar/buscar)

### Notificaciones
- ✅ Solo lectura (generadas por backend)
- ✅ Tipos: stock_bajo, venta_registrada, producto_actualizado
- ✅ Badge con contador de no leídas
- ✅ WebSocket para notificaciones en tiempo real

### Restricciones
- ❌ Sin acceso a gastos
- ❌ Sin acceso a analítica
- ❌ Sin acceso a reportes
- ❌ Sin acceso a gestión de usuarios
- ❌ Sin acceso a predicciones ML

---

## 🎨 UI/UX RECOMENDACIONES CON TAILWIND CSS

1. **Colores (Tailwind):** 
   - Primary: `bg-blue-600 hover:bg-blue-700 text-white`
   - Success: `bg-green-600 hover:bg-green-700 text-white`
   - Danger: `bg-red-600 hover:bg-red-700 text-white`
   - Warning: `bg-yellow-500 hover:bg-yellow-600 text-white`
   
2. **Layout:**
   - Navbar superior con logo, nombre de tienda, notificaciones, logout
   - Sidebar izquierdo (desktop) o menú hamburguesa (mobile) con:
     - 🏠 Inicio (Dashboard simple)
     - 📦 Productos (Gestión completa)
     - 💰 Ventas (Registrar + historial)
     - 🔔 Notificaciones
   - Contenido principal con padding: `<main className="p-6">`
   
3. **Componentes UI con Tailwind:**
   ```jsx
   // Button.jsx
   <button className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">
     {children}
   </button>
   
   // Input.jsx
   <input className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" />
   
   // Card.jsx
   <div className="bg-white rounded-lg shadow-md p-6">
     {children}
   </div>
   
   // Table.jsx
   <table className="min-w-full divide-y divide-gray-200">
     <thead className="bg-gray-50">
       <tr>
         <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
           Código
         </th>
       </tr>
     </thead>
   </table>
   ```

4. **Icons:** Heroicons (ya compatible con Tailwind) o React Icons
   ```bash
   npm install @heroicons/react
   ```
   
5. **Forms:** React Hook Form para validaciones
   ```bash
   npm install react-hook-form
   ```
   
6. **Toasts:** react-hot-toast (ligero y compatible con Tailwind)
   ```bash
   npm install react-hot-toast
   ```
   
7. **Loading:** Spinner con Tailwind
   ```jsx
   <div className="flex items-center justify-center">
     <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
   </div>
   ```
   
8. **Empty States:** Centrados con Tailwind
   ```jsx
   <div className="flex flex-col items-center justify-center h-64 text-gray-500">
     <svg className="w-16 h-16 mb-4" />  
     <p className="text-lg">No hay productos registrados</p>
     <button className="mt-4 px-4 py-2 bg-blue-600 text-white rounded-lg">
       Crear Producto
     </button>
   </div>
   ```

9. **Responsive:** Usar clases responsive de Tailwind
   ```jsx
   <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
     {/* Cards de productos */}
   </div>
   ```

10. **Dark Mode (opcional):** Tailwind soporta dark mode out-of-the-box
    ```jsx
    <div className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
    ```

---

## ⚠️ ERRORES COMUNES A EVITAR

1. **NO uses UUIDs**, solo códigos (T001P001, T001V005)
2. **NO envíes fecha/hora** desde frontend (el backend decide)
3. **NO calcules totales** en frontend (usa POST /ventas/calcular)
4. **NO guardes tenant_id** manualmente (sale del JWT automáticamente)
5. **NO permitas stock negativo** en formularios
6. **NO permitas editar/eliminar ventas** (solo productos)
7. **NO llames a ActualizarAnalitica** (es solo EventBridge)
8. **NO uses HTTP en producción** (solo HTTPS para WebSockets)

---

## 🔧 CONFIGURACIÓN INICIAL

```javascript
// src/utils/constants.js
export const API_URL = import.meta.env.VITE_API_URL || 'https://xxx.execute-api.us-east-1.amazonaws.com/dev';
export const WS_URL = import.meta.env.VITE_WS_URL || 'wss://xxx.execute-api.us-east-1.amazonaws.com/dev';

export const CATEGORIAS = [
  'bebidas',
  'snacks',
  'abarrotes',
  'limpieza',
  'otros'
];

export const METODOS_PAGO = [
  'efectivo',
  'tarjeta',
  'yape'
];

// Estados permitidos (referencia, el backend maneja la lógica)
export const ESTADOS = {
  ACTIVO: 'ACTIVO',
  INACTIVO: 'INACTIVO'
};
```

```javascript
// .env.local (crear este archivo)
VITE_API_URL=https://xxx.execute-api.us-east-1.amazonaws.com/dev
VITE_WS_URL=wss://xxx.execute-api.us-east-1.amazonaws.com/dev
```

```javascript
// tailwind.config.js (crear si no existe)
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
        },
      },
    },
  },
  plugins: [],
}
```

---

## ✅ CHECKLIST DE IMPLEMENTACIÓN

Usa este checklist para trackear progreso:

- [ ] LoginPage + AuthContext
- [ ] API Service con interceptors
- [ ] Navegación principal (React Router con Sidebar o Navbar)
- [ ] ListaProductosPage con paginación
- [ ] CrearProductoPage con validaciones
- [ ] EditarProductoPage
- [ ] BuscarProductosPage
- [ ] RegistrarVentaPage con cálculo previo
- [ ] ListaVentasPage
- [ ] BuscarVentasPage
- [ ] NotificacionesPage
- [ ] WebSocket service
- [ ] Error handling global (react-hot-toast)
- [ ] Loading states (spinners Tailwind)
- [ ] Infinite scroll o botón Cargar más
- [ ] Testing en navegador (Chrome DevTools, responsive mode)

---

## 🚦 COMENZAR AHORA

**Primer paso:** Lee SAAI_oficial.txt sección "2. Arquitectura TRABAJADOR" completa.

**Segundo paso:** Revisa la estructura de carpetas de la plantilla React + Vite con Tailwind CSS.

**Tercer paso:** Implementa LoginPage + AuthContext (FASE 1.1 y 1.2).

**Pregunta antes de continuar:** ¿Entendiste el flujo de autenticación y cómo se usa el JWT en todas las requests?

---

¡Empecemos! Dime cuando hayas leído los archivos de referencia y estés listo para implementar el LoginPage.
